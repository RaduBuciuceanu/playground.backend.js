import { getRecords } from 'data/get-records'
import { createRecord } from 'data/create-record'
import { deleteRecord } from 'data/delete-record'

export const data = {
  createRecord,
  getRecords,
  deleteRecord
}

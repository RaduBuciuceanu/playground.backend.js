import { getRecords } from 'validations/get-records'
import { createRecord } from 'validations/create-record'
import { deleteRecord } from 'validations/delete-record'

export const validations = {
  createRecord,
  getRecords,
  deleteRecord
}
